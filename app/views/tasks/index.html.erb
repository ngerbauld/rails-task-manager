<h1>Tasks</h1>
<% @tasks.each do |task| %>
<li>

  <%= check_box_tag(:completed, task.completed, checked = task.completed) %>
  <%= link_to task.title, task_path(task) %>
  <%= link_to "Delete task", task_path(task.id), method: :delete %>
  <%= task.title %>
</li>
<% end %>
<p><%= link_to "Add new task", tasks_new_path %></p>



